import 'package:fastfood/core/constants/constant.dart';
import 'package:flutter/material.dart';

class Theme_app {
  static ThemeData theme_app = ThemeData(
        colorScheme: const ColorScheme.light(
          primary: ColorConst.KprimaryColor
        )
      );
}